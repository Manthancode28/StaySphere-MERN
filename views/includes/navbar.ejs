<style>
    .btn-search {
        background-color: #fe424d;
        color: #fff;
        border-radius: 1.5rem;
    }
    
    .btn-search:hover {
        background-color: #fe424d;
        color: #fff;
    }
    
    .btn-search:active {
        background-color: #fe424d;
        color: #fff;
    }
    
    .btn-search i {
        display: inline;
        font-size: 80%;
        margin-right: 0.6rem;
    }
    
    .inp-search {
        padding: 0.3rem 3rem 0.3rem 3rem;
        border-radius: 1.5rem;
    }
    
    .newNavbar {
        background-color: #fff;
        gap: 30px;
    }
</style>


<nav class="navbar navbar-expand-md sticky-top bg-body-light border-bottom ">
    <div class="container-fluid">
        <a class="navbar-brand" href="/listings"><i class="fa-solid fa-compass"></i></a>
        <button class="navbar-toggler " type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <!-- Basic Option -->
            <div class="navbar-nav">
                <a class="nav-link" href="/listings"> Explore</a>

            </div>
            <form class="d-flex ms-auto" role="search" action="/listings/search" method="GET">
                <label for="location" class="form-label"></label>
                <input class="form-control me-2 inp-search" type="search" placeholder="Search" aria-label="Search" name="location" id="location">

                <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
            </form>
            <!-- User Authentication -->
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/listings/new">Add New Listings</a>
                <% if( !currUser) { %>
                    <a class="nav-link" href="/signup"> <b> Sign Up</b></a>
                    <a class="nav-link" href="/login"><b>Log in</b> </a>
                    <% } %>

                        <% if( currUser) { %>
                            <a class="nav-link" href="/logout"> <b> Log out</b></a>
                            <% } %>

            </div>
        </div>
    </div>
</nav>